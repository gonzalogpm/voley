
import { DataProvider } from '../types';

/**
 * FUTURE IMPLEMENTATION:
 * When connecting Supabase:
 * 1. Implement this interface using @supabase/supabase-js
 * 2. Update DataContext.tsx to use this provider instead of localProvider
 * 3. Add offline sync logic here if needed (using local storage as cache)
 */
export const supabaseProvider: Partial<DataProvider> = {
  // Methods to be implemented...
};
